# Krafslösaskogen / Snurrom Survey

Before running:
1. Add the first-question illustration image as `public/nature-connectedness.jpg`
2. Install dependencies: `npm install`
3. Start locally: `npm start`

Push to GitHub:
```bash
git add .
git commit -m "Update survey and mobile layout"
git push
```

Render:
- Build command: `npm install`
- Start command: `npm start`
- Optional environment variable for persistent DB:
  - `DB_PATH=/var/data/survey.db`
